# hv_ Rename + JTCVS Merged-Header Table Engine Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rename `hvtiRtables`'s three exports to the `hv_` convention, then add a second table-construction mode — `hv_man_table_jtcvs()` / `hv_man_table_save_jtcvs()` — that reproduces the 2-row merged spanning-header structure, bold-italic section rows, lettered footnotes, and `Key:` line seen in Tess's real JTCVS template (`template_jtcvs_consolidated.docx`), as a mode separate from the existing flat-header `hv_man_table()`.

**Architecture:** Two new internal building blocks feed the new public functions: `.reshape_jtcvs_body()` turns a `gtsummary` table's `table_body` (built with an `{N_obs} ||| {stat}` glue statistic) into a data frame with paired N/stat columns per group and a `is_section` flag; `hv_man_table_jtcvs()` turns that into a `flextable` with a 2-row `add_header_row()`-merged header and `merge_h()`-collapsed, bold-italic section rows. `hv_man_table_save_jtcvs()` writes it to `.docx` with a bold caption paragraph and cell-targeted lettered footnotes, reusing a newly-extracted `.add_abbreviations_key()` helper shared with the renamed `hv_man_table_save()`.

**Tech Stack:** R, `gtsummary` (>= 2.5.0), `flextable` (>= 0.9.0), `officer` (>= 0.6.0), `testthat` (edition 3), `python-docx` (verification only, not a package dependency).

## Global Constraints

- House font: Times New Roman, 12pt (11pt permitted for wide tables) — same rule `hv_man_table()` already enforces; the JTCVS engine reuses it.
- Footnote symbols for the *existing* flat-header save function stay `c("*", "†", "‡", "§", "¶", "||")` — unchanged. The new JTCVS save function uses **lettered** footnotes (`a`, `b`, `c`, ...) instead — a different mechanism, not an extension of the symbol set.
- No CRAN release exists yet (`DESCRIPTION` version `0.1.0`, unmerged branch) — renaming exports is a breaking change with no external users to migrate; do not add back-compat aliases for the old names.
- Per `docs/superpowers/specs/2026-07-15-jtcvs-table-format-design.md`: one flexible engine, not per-archetype presets. Do not add `hv_table_baseline()`-style wrapper functions in this plan.
- Every new/changed exported function needs a `@return`/roxygen block sufficient for `R CMD check` (house release-gate habit, even though this plan doesn't run the full CRAN gate).

---

### Task 1: Rename existing exports to the `hv_` convention

**Files:**
- Modify (rename via `git mv`): `R/manuscript-flextable.R` → `R/hv-man-table.R`
- Modify (rename via `git mv`): `R/save-manuscript-table.R` → `R/hv-man-table-save.R`
- Modify (rename via `git mv`): `R/standard-footnotes.R` → `R/hv-man-footnotes.R`
- Modify (rename via `git mv`): `tests/testthat/test-manuscript-flextable.R` → `tests/testthat/test-hv-man-table.R`
- Modify (rename via `git mv`): `tests/testthat/test-save-manuscript-table.R` → `tests/testthat/test-hv-man-table-save.R`
- Modify: `README.md` (usage example)
- Modify: `NAMESPACE`, `man/manuscript_flextable.Rd`, `man/save_manuscript_table.Rd`, `man/standard_footnotes.Rd` (regenerated by roxygen2 in the last step, stale `.Rd` files removed)

**Interfaces:**
- Produces: `hv_man_table(tbl, font = "Times New Roman", font_size = 12, digits = 2)` (was `manuscript_flextable()`); `hv_man_table_save(ft, file, footnotes = hv_man_footnotes(), abbreviations = NULL)` (was `save_manuscript_table()`); `hv_man_footnotes()` (was `standard_footnotes()`). Signatures and bodies are otherwise byte-for-byte identical to today — this task only renames.

- [ ] **Step 1: Rename the three R source files and update their function names**

```bash
git mv R/manuscript-flextable.R R/hv-man-table.R
git mv R/save-manuscript-table.R R/hv-man-table-save.R
git mv R/standard-footnotes.R R/hv-man-footnotes.R
```

In `R/hv-man-table.R`, change the function declaration:
```r
manuscript_flextable <- function(tbl, font = "Times New Roman", font_size = 12,
                                 digits = 2) {
```
to
```r
hv_man_table <- function(tbl, font = "Times New Roman", font_size = 12,
                         digits = 2) {
```
(keep the roxygen block above it — update `@export`-adjacent title line from "Convert a gtsummary table into a Blackstone-compliant flextable" text only if it names the old function; it currently doesn't, so leave prose as-is except any literal `manuscript_flextable()` self-references, of which there are none in that file today.)

In `R/hv-man-table-save.R`, change:
```r
save_manuscript_table <- function(ft, file, footnotes = standard_footnotes(),
                                  abbreviations = NULL) {
```
to
```r
hv_man_table_save <- function(ft, file, footnotes = hv_man_footnotes(),
                              abbreviations = NULL) {
```
Also update the roxygen `@param footnotes` line and `@seealso` block, which reference `[standard_footnotes()]` and `[manuscript_flextable()]` — change both to `[hv_man_footnotes()]` and `[hv_man_table()]`.

In `R/hv-man-footnotes.R`, change:
```r
standard_footnotes <- function() {
```
to
```r
hv_man_footnotes <- function() {
```
Update the roxygen `@seealso [save_manuscript_table()]` to `@seealso [hv_man_table_save()]`.

- [ ] **Step 2: Rename the test files and update every call site inside them**

```bash
git mv tests/testthat/test-manuscript-flextable.R tests/testthat/test-hv-man-table.R
git mv tests/testthat/test-save-manuscript-table.R tests/testthat/test-hv-man-table-save.R
```

In `tests/testthat/test-hv-man-table.R`, replace every call `manuscript_flextable(` with `hv_man_table(` (5 occurrences: `mk_tbl()`'s caller sites in each `test_that` block plus the two `expect_error` calls).

In `tests/testthat/test-hv-man-table-save.R`, replace every call `save_manuscript_table(` with `hv_man_table_save(` (11 occurrences across the file) and `manuscript_flextable(` with `hv_man_table(` (inside `mk_ft()`).

- [ ] **Step 3: Run the test suite to confirm it fails on the old names being gone**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_dir("tests/testthat", filter = "hv-man-table")'`
Expected: FAIL — `could not find function "manuscript_flextable"` (or similar), because `NAMESPACE` and any remaining internal cross-references still say the old names until Step 4.

- [ ] **Step 4: Update NAMESPACE and regenerate docs**

```bash
Rscript -e 'roxygen2::roxygenise()'
```
This rewrites `NAMESPACE` to `export(hv_man_footnotes)`, `export(hv_man_table)`, `export(hv_man_table_save)`, and regenerates `man/hv_man_footnotes.Rd`, `man/hv_man_table.Rd`, `man/hv_man_table_save.Rd`. Delete the now-stale old `.Rd` files if roxygen2 doesn't remove them automatically:

```bash
rm -f man/manuscript_flextable.Rd man/save_manuscript_table.Rd man/standard_footnotes.Rd
```

- [ ] **Step 5: Update the README example**

In `README.md`, change:
```r
ft <- manuscript_flextable(tbl)
save_manuscript_table(ft, "table1.docx")
```
to
```r
ft <- hv_man_table(tbl)
hv_man_table_save(ft, "table1.docx")
```

- [ ] **Step 6: Run the full test suite and confirm it passes**

Run: `Rscript -e 'devtools::load_all("."); devtools::test()'`
Expected: All tests PASS (0 failures), including the two renamed files.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "refactor: rename exports to hv_ convention (manuscript_flextable -> hv_man_table, save_manuscript_table -> hv_man_table_save, standard_footnotes -> hv_man_footnotes)"
```

---

### Task 2: `.reshape_jtcvs_body()` — internal N/stat-pair reshape helper

**Files:**
- Create: `R/hv-man-table-jtcvs.R` (this task adds only the internal helper; `hv_man_table_jtcvs()` itself is Task 3, same file)
- Test: `tests/testthat/test-hv-man-table-jtcvs.R`

**Interfaces:**
- Consumes: a `gtsummary` object whose `table_body` has one `stat_<k>` column per group, each cell formatted as `"<N_obs> ||| <display>"` (caller builds this via `tbl_summary(statistic = list(... ~ "{N_obs} ||| {stat}"))`).
- Produces: `.reshape_jtcvs_body(tbl, groups)` returns a `data.frame` with columns `label`, `is_section` (logical), `row_type` (character, gtsummary's own `"label"`/`"level"`), and for every name in `groups` two columns `n_<name>` and `disp_<name>` (e.g. `groups = c(stat_1 = "...", stat_2 = "...")` produces `n_stat_1`, `disp_stat_1`, `n_stat_2`, `disp_stat_2`). `is_section` is `TRUE` for rows where `groupname_col` changes from the previous row (the section-header marker), and those rows carry the section label in `label` with `NA` in every `n_*`/`disp_*` column. Rows where a `stat_<k>` cell is `NA` (categorical parent rows with only child levels) get `NA` in both `n_<name>` and `disp_<name>` for that group. (Task 3 extends this function's signature to `.reshape_jtcvs_body(tbl, groups, trailing = NULL)` — not tested here, since `trailing` has no effect on any assertion in this task.)

- [ ] **Step 1: Write the failing test**

```r
# tests/testthat/test-hv-man-table-jtcvs.R
library(dplyr)
library(gtsummary)

mk_jtcvs_tbl <- function() {
  set.seed(42)
  n <- 60
  dta <- data.frame(
    group = factor(sample(c("A", "B"), n, replace = TRUE)),
    age = round(rnorm(n, 60, 10)),
    nyha = factor(sample(c("I", "II", "III"), n, replace = TRUE))
  )
  dta$age[sample(n, 5)] <- NA

  dta |>
    tbl_summary(
      by = group,
      statistic = list(
        all_continuous() ~ "{N_obs} ||| {mean} ± {sd}",
        all_categorical() ~ "{N_obs} ||| {n} ({p}%)"
      ),
      missing = "no"
    ) |>
    modify_table_body(
      mutate,
      groupname_col = case_when(variable == "age" ~ "Demographics", TRUE ~ "Cardiac")
    )
}

test_that(".reshape_jtcvs_body splits N_obs and stat into paired columns", {
  reshaped <- hvtiRtables:::.reshape_jtcvs_body(
    mk_jtcvs_tbl(), groups = c(stat_1 = "Group A", stat_2 = "Group B")
  )
  age_row <- reshaped[reshaped$label == "age", ]
  expect_identical(age_row$n_stat_1, "27")
  expect_true(grepl("±", age_row$disp_stat_1))
  expect_identical(age_row$n_stat_2, "33")
})

test_that(".reshape_jtcvs_body marks section-header rows and blanks their stats", {
  reshaped <- hvtiRtables:::.reshape_jtcvs_body(
    mk_jtcvs_tbl(), groups = c(stat_1 = "Group A", stat_2 = "Group B")
  )
  sec <- reshaped[reshaped$is_section, ]
  expect_identical(sec$label, c("Demographics", "Cardiac"))
  expect_true(all(is.na(sec$n_stat_1)))
  expect_true(all(is.na(sec$disp_stat_1)))
})

test_that(".reshape_jtcvs_body leaves categorical parent rows blank, not erroring", {
  reshaped <- hvtiRtables:::.reshape_jtcvs_body(
    mk_jtcvs_tbl(), groups = c(stat_1 = "Group A", stat_2 = "Group B")
  )
  nyha_row <- reshaped[reshaped$label == "nyha", ]
  expect_true(is.na(nyha_row$n_stat_1))
})

test_that(".reshape_jtcvs_body works when groupname_col was never set (no sections)", {
  set.seed(42)
  n <- 60
  dta <- data.frame(
    group = factor(sample(c("A", "B"), n, replace = TRUE)),
    age = round(rnorm(n, 60, 10))
  )
  dta$age[sample(n, 5)] <- NA
  tbl <- dta |> tbl_summary(
    by = group,
    statistic = list(all_continuous() ~ "{N_obs} ||| {mean} ± {sd}"),
    missing = "no"
  )
  expect_false("groupname_col" %in% names(tbl$table_body))
  reshaped <- hvtiRtables:::.reshape_jtcvs_body(tbl, groups = c(stat_1 = "Group A", stat_2 = "Group B"))
  expect_false(any(reshaped$is_section))
  expect_identical(nrow(reshaped), nrow(tbl$table_body))
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-hv-man-table-jtcvs.R")'`
Expected: FAIL with `could not find function ".reshape_jtcvs_body"`.

- [ ] **Step 3: Write the implementation**

`groupname_col` only exists in `table_body` when the caller has called
`modify_table_body(mutate, groupname_col = ...)` — a table with no section
grouping (e.g. a flat outcomes table) simply doesn't have that column.
Verified during planning: indexing `tb$groupname_col` when the column is
absent returns `NULL`, and the section-insertion logic below errors on it
(`rbind`: "numbers of columns of arguments do not match"). Guard on
`has_sections` so the no-grouping case is a no-op rather than an error —
this case is exercised directly in Task 4's test fixture.

```r
# R/hv-man-table-jtcvs.R (top of file)

.reshape_jtcvs_body <- function(tbl, groups) {
  tb <- tbl$table_body
  has_sections <- "groupname_col" %in% names(tb)

  out <- data.frame(
    label = tb$label,
    is_section = FALSE,
    row_type = tb$row_type,
    stringsAsFactors = FALSE
  )

  for (col in names(groups)) {
    parts <- strsplit(tb[[col]], " \\|\\|\\| ")
    out[[paste0("n_", col)]] <- vapply(parts, function(p) if (length(p) == 2) p[1] else NA_character_, character(1))
    out[[paste0("disp_", col)]] <- vapply(parts, function(p) if (length(p) == 2) p[2] else NA_character_, character(1))
  }

  if (!has_sections) {
    rownames(out) <- NULL
    return(out)
  }

  # a row is a "new section" if this is the first row, or groupname_col
  # changed since the previous row
  is_section <- c(TRUE, tb$groupname_col[-1] != tb$groupname_col[-nrow(tb)])

  # Insert one section-header row before each run of same-groupname_col rows
  section_starts <- which(is_section)
  section_labels <- tb$groupname_col[section_starts]
  sec_rows <- out[rep(NA_integer_, length(section_starts)), , drop = FALSE]
  sec_rows$label <- section_labels
  sec_rows$is_section <- TRUE
  sec_rows$row_type <- "section"
  stat_cols <- setdiff(names(out), c("label", "is_section", "row_type"))
  sec_rows[stat_cols] <- NA_character_

  insert_before <- section_starts
  result <- out[0, , drop = FALSE]
  prev <- 0L
  for (k in seq_along(insert_before)) {
    if (insert_before[k] > prev + 1L) {
      result <- rbind(result, out[(prev + 1L):(insert_before[k] - 1L), , drop = FALSE])
    }
    result <- rbind(result, sec_rows[k, , drop = FALSE])
    prev <- insert_before[k] - 1L
  }
  if (prev < nrow(out)) {
    result <- rbind(result, out[(prev + 1L):nrow(out), , drop = FALSE])
  }
  rownames(result) <- NULL
  result
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-hv-man-table-jtcvs.R")'`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add R/hv-man-table-jtcvs.R tests/testthat/test-hv-man-table-jtcvs.R
git commit -m "feat: .reshape_jtcvs_body() internal helper for JTCVS N/stat column pairing"
```

---

### Task 3: `hv_man_table_jtcvs()` — the merged-header flextable engine

**Files:**
- Modify: `R/hv-man-table-jtcvs.R` (extend `.reshape_jtcvs_body()` from Task 2 with a `trailing` parameter — it must run through the same section-row insertion as the other columns, not be bolted on after — then add the exported `hv_man_table_jtcvs()` below it)
- Test: `tests/testthat/test-hv-man-table-jtcvs.R` (append)

**Interfaces:**
- Consumes: `.reshape_jtcvs_body(tbl, groups, trailing = NULL)` (extended signature; Task 2's version only had `tbl, groups`).
- Produces: `hv_man_table_jtcvs(tbl, groups, trailing = NULL, font = "Times New Roman", font_size = 12)` returning a `flextable`. `groups` is a named character vector, `stat_<k>` column name -> spanning header label (must include the group's own N in the label text, e.g. `c(stat_1 = "Group A (n=60)")` — this function does not compute or inject the N into the label itself). `trailing` is an optional named character vector of length 1, `<column name already in tbl$table_body>` -> header label (e.g. `c(std_diff = "Std. Diff.")`); the column must already exist (caller adds it via `modify_table_body()` beforehand).

- [ ] **Step 1: Write the failing test**

Note on approach: `flextable`'s `$header$dataset` / `$body$dataset` fields hold pre-render placeholder data, not the labels set via `set_header_labels()`/`add_header_row()` — verified by rendering and inspecting the actual `.docx` XML. Header-content assertions below render to a temp `.docx` and check the XML text, matching the `docx_xml()`-style helper this package's other tests already use. Body-content assertions (which *do* reflect real values, since body cells aren't relabeled) use `ft$body$dataset` and `ft$body$styles$text$bold$data` directly — both verified against a real rendered table this session.

```r
docx_xml_jtcvs <- function(ft) {
  out <- tempfile(fileext = ".docx")
  on.exit(unlink(out), add = TRUE)
  flextable::save_as_docx(ft, path = out)
  xdir <- tempfile()
  utils::unzip(out, exdir = xdir)
  paste(readLines(file.path(xdir, "word", "document.xml"), warn = FALSE), collapse = "")
}

test_that("hv_man_table_jtcvs builds a 2-row header with merged group spans", {
  ft <- hv_man_table_jtcvs(
    mk_jtcvs_tbl(),
    groups = c(stat_1 = "Group A (n=27)", stat_2 = "Group B (n=33)")
  )
  expect_s3_class(ft, "flextable")
  expect_identical(flextable::nrow_part(ft, "header"), 2L)

  xml <- docx_xml_jtcvs(ft)
  expect_true(grepl("Group A (n=27)", xml, fixed = TRUE))
  expect_true(grepl("Group B (n=33)", xml, fixed = TRUE))
  expect_true(grepl(">na<", xml, fixed = TRUE))
  expect_true(grepl("No. (%) or Mean", xml, fixed = TRUE))
  # sub-header row ("na") comes after the spanning group-label row in document order
  expect_true(regexpr("Group A", xml, fixed = TRUE) < regexpr(">na<", xml, fixed = TRUE))
})

test_that("hv_man_table_jtcvs bolds and merges section-header rows", {
  ft <- hv_man_table_jtcvs(
    mk_jtcvs_tbl(),
    groups = c(stat_1 = "Group A (n=27)", stat_2 = "Group B (n=33)")
  )
  body <- ft$body$dataset
  sec_i <- which(body$label %in% c("Demographics", "Cardiac"))
  expect_length(sec_i, 2L)
  bold_map <- ft$body$styles$text$bold$data
  expect_true(all(bold_map[sec_i, 1]))
})

test_that("hv_man_table_jtcvs adds an optional trailing column", {
  tbl <- mk_jtcvs_tbl()
  tbl$table_body$std_diff <- "12"
  ft <- hv_man_table_jtcvs(
    tbl,
    groups = c(stat_1 = "Group A (n=27)", stat_2 = "Group B (n=33)"),
    trailing = c(std_diff = "Std. Diff.")
  )
  expect_true("std_diff" %in% ft$col_keys)
  xml <- docx_xml_jtcvs(ft)
  expect_true(grepl("Std. Diff.", xml, fixed = TRUE))
})

test_that("hv_man_table_jtcvs applies the house font", {
  ft <- hv_man_table_jtcvs(
    mk_jtcvs_tbl(),
    groups = c(stat_1 = "Group A (n=27)", stat_2 = "Group B (n=33)")
  )
  xml <- docx_xml_jtcvs(ft)
  expect_true(grepl("Times New Roman", xml, fixed = TRUE))
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-hv-man-table-jtcvs.R")'`
Expected: FAIL with `could not find function "hv_man_table_jtcvs"`.

- [ ] **Step 3: Write the implementation**

First, extend `.reshape_jtcvs_body()` from Task 2 with a `trailing` parameter. The trailing column must be threaded through the *same* section-row insertion logic as the `n_`/`disp_` columns — bolting it on afterward (by assigning `tbl$table_body[[trailing_col]]` directly into the reshaped frame) mismatches row counts once section-header rows have been inserted, since `tbl$table_body` has no such rows. This was caught by actually running the code during planning, not left as a hypothetical:

```r
# R/hv-man-table-jtcvs.R — modify .reshape_jtcvs_body() from Task 2

.reshape_jtcvs_body <- function(tbl, groups, trailing = NULL) {
  tb <- tbl$table_body
  has_sections <- "groupname_col" %in% names(tb)

  out <- data.frame(
    label = tb$label,
    is_section = FALSE,
    row_type = tb$row_type,
    stringsAsFactors = FALSE
  )

  for (col in names(groups)) {
    parts <- strsplit(tb[[col]], " \\|\\|\\| ")
    out[[paste0("n_", col)]] <- vapply(parts, function(p) if (length(p) == 2) p[1] else NA_character_, character(1))
    out[[paste0("disp_", col)]] <- vapply(parts, function(p) if (length(p) == 2) p[2] else NA_character_, character(1))
  }

  if (!is.null(trailing)) {
    out[[names(trailing)]] <- tb[[names(trailing)]]
  }

  if (!has_sections) {
    rownames(out) <- NULL
    return(out)
  }

  is_section <- c(TRUE, tb$groupname_col[-1] != tb$groupname_col[-nrow(tb)])

  # Insert one section-header row before each run of same-groupname_col rows
  section_starts <- which(is_section)
  section_labels <- tb$groupname_col[section_starts]
  sec_rows <- out[rep(NA_integer_, length(section_starts)), , drop = FALSE]
  sec_rows$label <- section_labels
  sec_rows$is_section <- TRUE
  sec_rows$row_type <- "section"
  stat_cols <- setdiff(names(out), c("label", "is_section", "row_type"))
  sec_rows[stat_cols] <- NA_character_

  insert_before <- section_starts
  result <- out[0, , drop = FALSE]
  prev <- 0L
  for (k in seq_along(insert_before)) {
    if (insert_before[k] > prev + 1L) {
      result <- rbind(result, out[(prev + 1L):(insert_before[k] - 1L), , drop = FALSE])
    }
    result <- rbind(result, sec_rows[k, , drop = FALSE])
    prev <- insert_before[k] - 1L
  }
  if (prev < nrow(out)) {
    result <- rbind(result, out[(prev + 1L):nrow(out), , drop = FALSE])
  }
  rownames(result) <- NULL
  result
}
```

Then add the exported engine below it:

```r
# R/hv-man-table-jtcvs.R (below the extended .reshape_jtcvs_body)

#' Build a JTCVS-format manuscript table with merged spanning headers
#'
#' Companion to [hv_man_table()] for the shape editorial actually needs at
#' JTCVS submission: a 2-row header (group name spanning `na`/stat
#' sub-columns) and bold-italic, row-spanning section headers in the body,
#' matching Tess's JTCVS template. This is a separate mode, not a
#' replacement for [hv_man_table()]'s flat-header CORR house style.
#'
#' @param tbl A `gtsummary` table object whose `statistic` argument used
#'   `"{N_obs} ||| {<stat>}"` for every group column (see `groups`).
#' @param groups Named character vector, `stat_<k>` column name in
#'   `tbl$table_body` -> spanning header label (include the group's N in
#'   the label text yourself, e.g. `c(stat_1 = "Group A (n=60)")`).
#' @param trailing Optional named character vector of length 1, an existing
#'   `tbl$table_body` column name -> header label, for a trailing
#'   comparison column (e.g. `c(std_diff = "Std. Diff.")` or
#'   `c(p_value = "P")`). Must already exist in `tbl$table_body`.
#' @param font Font family. Default `"Times New Roman"` (house rule).
#' @param font_size Font size in points. Default `12`; pass `11` for wide
#'   tables.
#'
#' @return A `flextable` with a 2-row header and merged section rows, ready
#'   for [hv_man_table_save_jtcvs()].
#'
#' @seealso [hv_man_table()] for the flat-header CORR house-style mode.
#'   [hv_man_table_save_jtcvs()] to write the result to a compliant `.docx`.
#'
#' @export
hv_man_table_jtcvs <- function(tbl, groups, trailing = NULL,
                               font = "Times New Roman", font_size = 12) {
  if (!inherits(tbl, "gtsummary"))
    stop("`tbl` must be a gtsummary table object.", call. = FALSE)
  if (is.null(names(groups)) || any(!nzchar(names(groups))))
    stop("`groups` must be a named character vector.", call. = FALSE)

  reshaped <- .reshape_jtcvs_body(tbl, groups, trailing)

  n_cols <- unlist(lapply(names(groups), function(g) paste0("n_", g)))
  disp_cols <- unlist(lapply(names(groups), function(g) paste0("disp_", g)))
  interleaved <- as.vector(rbind(n_cols, disp_cols))
  col_keys <- c("label", interleaved, if (!is.null(trailing)) names(trailing))

  ft <- flextable::flextable(reshaped, col_keys = col_keys)

  header_labels <- list(label = "Characteristic")
  for (g in names(groups)) {
    header_labels[[paste0("n_", g)]] <- "na"
    header_labels[[paste0("disp_", g)]] <- "No. (%) or Mean ± SD"
  }
  if (!is.null(trailing)) header_labels[[names(trailing)]] <- unname(trailing)
  ft <- do.call(flextable::set_header_labels, c(list(x = ft), header_labels))

  top_values <- c("Characteristic", unname(groups))
  top_widths <- c(1L, rep(2L, length(groups)))
  if (!is.null(trailing)) {
    top_values <- c(top_values, "")
    top_widths <- c(top_widths, 1L)
  }
  ft <- flextable::add_header_row(ft, top = TRUE, values = top_values, colwidths = top_widths)

  sec_i <- which(reshaped$is_section)
  ft <- flextable::merge_h(ft, i = sec_i, part = "body")
  ft <- flextable::bold(ft, i = sec_i, part = "body", bold = TRUE)
  ft <- flextable::italic(ft, i = sec_i, part = "body", italic = TRUE)

  level_i <- which(reshaped$row_type == "level")
  if (length(level_i) > 0)
    ft <- flextable::padding(ft, i = level_i, j = "label", padding.left = 20)

  ft <- flextable::font(ft, fontname = font, part = "all")
  ft <- flextable::fontsize(ft, size = font_size, part = "all")
  ft <- flextable::valign(ft, valign = "center", part = "all")

  ft
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-hv-man-table-jtcvs.R")'`
Expected: PASS (7 tests total: 3 from Task 2 + 4 from this task).

- [ ] **Step 5: Commit**

```bash
git add R/hv-man-table-jtcvs.R tests/testthat/test-hv-man-table-jtcvs.R
git commit -m "feat: hv_man_table_jtcvs() - merged-header JTCVS table engine"
```

---

### Task 4: `hv_man_table_save_jtcvs()` — caption, lettered footnotes, shared Key: helper

**Files:**
- Modify: `R/hv-man-table-save.R` (extract `.add_abbreviations_key()`, use it from `hv_man_table_save()`)
- Create: `R/hv-man-table-save-jtcvs.R`
- Test: `tests/testthat/test-hv-man-table-save-jtcvs.R`

**Interfaces:**
- Consumes: `flextable` from `hv_man_table_jtcvs()` (Task 3); `.add_abbreviations_key(doc, abbreviations)` (new internal helper, extracted this task).
- Produces: `hv_man_table_save_jtcvs(ft, file, caption, footnotes = NULL, abbreviations = NULL)`. `caption` is the full literal text (e.g. `"Table 1. Baseline and Operative Characteristics"`) rendered bold above the table. `footnotes` is a list of `list(row = <body row index>, col = <col_key>, text = "...")`, rendered in list order as lettered (`a`, `b`, `c`, ...) superscript markers attached to the given body cell, with the footnote text as its own paragraph below the table (same lettered prefix).

- [ ] **Step 1: Write the failing test for the shared helper extraction (no behavior change)**

```r
# tests/testthat/test-hv-man-table-save.R (append to existing file)
test_that("hv_man_table_save still renders the Key: block after the internal refactor", {
  ft <- mk_ft()
  f <- tempfile(fileext = ".docx")
  on.exit(unlink(f), add = TRUE)
  hv_man_table_save(ft, f, abbreviations = c(NYHA = "New York Heart Association"))
  xml <- read_docx_text(f)
  expect_true(grepl("Key:", xml, fixed = TRUE))
  expect_true(grepl("NYHA", xml, fixed = TRUE))
})
```

- [ ] **Step 2: Run test to verify it currently passes (baseline, no regression yet)**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-hv-man-table-save.R")'`
Expected: PASS (this behavior already exists; this step just locks it in before refactoring).

- [ ] **Step 3: Extract `.add_abbreviations_key()` from `hv_man_table_save()`**

In `R/hv-man-table-save.R`, replace the abbreviations block (the `if (!is.null(abbreviations) ...)` block) with a call to a new internal helper, and define that helper at the bottom of the file:

```r
  doc <- .add_abbreviations_key(doc, abbreviations)

  print(doc, target = file)
  invisible(file)
}

.add_abbreviations_key <- function(doc, abbreviations) {
  if (is.null(abbreviations) || length(abbreviations) == 0) return(doc)
  abbr_names <- names(abbreviations)
  if (is.null(abbr_names) || any(!nzchar(abbr_names)))
    stop("`abbreviations` must be a named character vector ",
         "(c(ABBR = \"expansion\", ...)); every element must have a ",
         "non-empty name.", call. = FALSE)
  ordered <- abbreviations[order(names(abbreviations))]
  runs <- list(officer::ftext("Key: "))
  italic_prop <- officer::fp_text(italic = TRUE)
  for (i in seq_along(ordered)) {
    runs <- c(runs, list(officer::ftext(names(ordered)[i], prop = italic_prop)))
    suffix <- if (i < length(ordered)) paste0(", ", ordered[i], "; ") else paste0(", ", ordered[i])
    runs <- c(runs, list(officer::ftext(suffix)))
  }
  key_par <- do.call(officer::fpar, runs)
  officer::body_add_fpar(doc, key_par, style = "Normal")
}
```

Remove the now-duplicated inline block from the function body, keeping everything above it (the flextable insertion and symbol-footnote loop) unchanged.

- [ ] **Step 4: Run test to verify no regression**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-hv-man-table-save.R")'`
Expected: PASS, same test count as Step 2 (the extraction is behavior-preserving).

- [ ] **Step 5: Commit the refactor separately**

```bash
git add R/hv-man-table-save.R tests/testthat/test-hv-man-table-save.R
git commit -m "refactor: extract .add_abbreviations_key() from hv_man_table_save() for reuse"
```

- [ ] **Step 6: Write the failing test for the new JTCVS save function**

```r
# tests/testthat/test-hv-man-table-save-jtcvs.R
library(dplyr)
library(gtsummary)

mk_jtcvs_ft <- function() {
  set.seed(42)
  n <- 60
  dta <- data.frame(
    group = factor(sample(c("A", "B"), n, replace = TRUE)),
    age = round(rnorm(n, 60, 10))
  )
  dta$age[sample(n, 5)] <- NA
  tbl <- dta |> tbl_summary(
    by = group,
    statistic = list(all_continuous() ~ "{N_obs} ||| {mean} ± {sd}"),
    missing = "no"
  )
  hv_man_table_jtcvs(tbl, groups = c(stat_1 = "Group A (n=27)", stat_2 = "Group B (n=33)"))
}

read_docx_text <- function(path) {
  xdir <- tempfile()
  utils::unzip(path, exdir = xdir)
  paste(readLines(file.path(xdir, "word", "document.xml"), warn = FALSE), collapse = "")
}

test_that("hv_man_table_save_jtcvs renders a bold caption before the table", {
  ft <- mk_jtcvs_ft()
  f <- tempfile(fileext = ".docx")
  on.exit(unlink(f), add = TRUE)
  hv_man_table_save_jtcvs(ft, f, caption = "Table 1. Baseline Characteristics")
  xml <- read_docx_text(f)
  expect_true(grepl("Table 1. Baseline Characteristics", xml, fixed = TRUE))
  # flextable/officer emit `<w:b w:val="true"/>`, not the bare `<w:b/>` form —
  # verified against actual rendered XML during planning. Check bold appears
  # in the run immediately preceding the caption text, not just anywhere in
  # the document (the table body also has bold section-header cells).
  cap_pos <- regexpr("Table 1. Baseline Characteristics", xml, fixed = TRUE)
  preceding_run <- substr(xml, max(1, cap_pos - 300), cap_pos)
  expect_true(grepl('w:b w:val="true"', preceding_run, fixed = TRUE))
})

test_that("hv_man_table_save_jtcvs attaches lettered footnotes to specific cells", {
  ft <- mk_jtcvs_ft()
  f <- tempfile(fileext = ".docx")
  on.exit(unlink(f), add = TRUE)
  hv_man_table_save_jtcvs(
    ft, f, caption = "Table 1. Baseline Characteristics",
    footnotes = list(
      list(row = 1, col = "n_stat_1", text = "Patients with data available."),
      list(row = 1, col = "n_stat_2", text = "Second footnote.")
    )
  )
  xml <- read_docx_text(f)
  expect_true(grepl("a\\. Patients with data available\\.|a Patients with data available", xml))
  expect_true(grepl("b\\. Second footnote\\.|b Second footnote", xml))
})

test_that("hv_man_table_save_jtcvs reuses the shared Key: helper", {
  ft <- mk_jtcvs_ft()
  f <- tempfile(fileext = ".docx")
  on.exit(unlink(f), add = TRUE)
  hv_man_table_save_jtcvs(
    ft, f, caption = "Table 1. Baseline Characteristics",
    abbreviations = c(SD = "standard deviation")
  )
  xml <- read_docx_text(f)
  expect_true(grepl("Key:", xml, fixed = TRUE))
  expect_true(grepl("SD", xml, fixed = TRUE))
})
```

- [ ] **Step 7: Run test to verify it fails**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-hv-man-table-save-jtcvs.R")'`
Expected: FAIL with `could not find function "hv_man_table_save_jtcvs"`.

- [ ] **Step 8: Write the implementation**

```r
# R/hv-man-table-save-jtcvs.R

#' Write an hv_man_table_jtcvs() table to a compliant .docx
#'
#' JTCVS counterpart to [hv_man_table_save()]: adds a bold `Table N.
#' Caption` paragraph before the table, and renders footnotes as lettered
#' (`a.`, `b.`, ...) markers attached to specific body cells rather than
#' the fixed symbol set [hv_man_table_save()] uses on the header row —
#' matching Tess's JTCVS template convention.
#'
#' @param ft A `flextable`, from [hv_man_table_jtcvs()].
#' @param file Output `.docx` path.
#' @param caption Full caption text, e.g. `"Table 1. Baseline
#'   Characteristics"` — rendered bold above the table. This function does
#'   not auto-number tables; include the number yourself.
#' @param footnotes Optional list of `list(row =, col =, text =)`, one per
#'   footnote, in the order letters should be assigned (`a`, `b`, ...).
#'   `row`/`col` address a body cell in `ft` (`col` is a `col_keys` name).
#' @param abbreviations Optional named character vector, same as
#'   [hv_man_table_save()] — rendered via the shared `Key:` helper.
#'
#' @return Invisibly, the `file` path.
#'
#' @seealso [hv_man_table_jtcvs()] to build a compliant `flextable` first.
#'
#' @export
hv_man_table_save_jtcvs <- function(ft, file, caption, footnotes = NULL,
                                    abbreviations = NULL) {
  if (!inherits(ft, "flextable"))
    stop("`ft` must be a flextable object.", call. = FALSE)
  if (!is.character(caption) || length(caption) != 1L || is.na(caption) || !nzchar(caption))
    stop("`caption` must be a single non-empty string.", call. = FALSE)
  out_dir <- dirname(file)
  if (!dir.exists(out_dir))
    stop("Output directory does not exist: ", out_dir, call. = FALSE)

  letters_seq <- letters
  if (!is.null(footnotes) && length(footnotes) > length(letters_seq))
    stop("Too many footnotes (max ", length(letters_seq), " letters).", call. = FALSE)

  if (!is.null(footnotes)) {
    for (k in seq_along(footnotes)) {
      fn <- footnotes[[k]]
      ft <- flextable::append_chunks(
        ft, i = fn$row, j = fn$col, part = "body",
        flextable::as_sup(letters_seq[k])
      )
    }
  }

  doc <- officer::read_docx()
  cap_par <- officer::fpar(officer::ftext(caption, prop = officer::fp_text(bold = TRUE)))
  doc <- officer::body_add_fpar(doc, cap_par, style = "Normal")
  doc <- flextable::body_add_flextable(doc, ft)

  if (!is.null(footnotes)) {
    for (k in seq_along(footnotes)) {
      sup_prop <- officer::fp_text(vertical.align = "superscript")
      note_par <- officer::fpar(
        officer::ftext(letters_seq[k], prop = sup_prop),
        officer::ftext(paste0(". ", footnotes[[k]]$text))
      )
      doc <- officer::body_add_fpar(doc, note_par, style = "Normal")
    }
  }

  doc <- .add_abbreviations_key(doc, abbreviations)

  print(doc, target = file)
  invisible(file)
}
```

- [ ] **Step 9: Run test to verify it passes**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-hv-man-table-save-jtcvs.R")'`
Expected: PASS (3 tests).

- [ ] **Step 10: Commit**

```bash
git add R/hv-man-table-save-jtcvs.R tests/testthat/test-hv-man-table-save-jtcvs.R
git commit -m "feat: hv_man_table_save_jtcvs() - bold caption + lettered cell-targeted footnotes"
```

---

### Task 5: Characterization test against the real template

**Files:**
- Test: `tests/testthat/test-hv-man-table-jtcvs.R` (append)

**Interfaces:**
- Consumes: `hv_man_table_jtcvs()` (Task 3). No production code changes in this task — fixture-only.

- [ ] **Step 1: Write the characterization test using real template values**

Values below are copied from `template_jtcvs_consolidated.docx` Table 0 (mitral valve re-replacement), rows 0-3, extracted this session via `python-docx`.

```r
test_that("hv_man_table_jtcvs reproduces the template's Table 1 header/section shape", {
  set.seed(1)
  n <- 525
  # `factor()` defaults to alphabetical level order ("Isolated" before
  # "Non-Isolated"), which would silently swap which group lands in stat_1
  # vs. stat_2 — verified by actually running this without explicit
  # `levels =` during planning and getting 133/392 reversed. Pin the levels
  # explicitly so stat_1 is deterministically "Non-Isolated".
  dta <- data.frame(
    group = factor(
      rep(c("Non-Isolated", "Isolated"), c(392, 133)),
      levels = c("Non-Isolated", "Isolated")
    ),
    age = round(rnorm(n, 66.6, 12))
  )
  tbl <- dta |> gtsummary::tbl_summary(
    by = group,
    statistic = list(gtsummary::all_continuous() ~ "{N_obs} ||| {mean} ± {sd}"),
    missing = "no"
  ) |>
    gtsummary::modify_table_body(dplyr::mutate, groupname_col = "Demographics")

  ft <- hv_man_table_jtcvs(
    tbl,
    groups = c(
      stat_1 = "Non-Isolated Re-Replacement (n=392)",
      stat_2 = "Isolated Re-Replacement (n=133)"
    )
  )
  xml <- docx_xml_jtcvs(ft)
  expect_true(grepl(">na<", xml, fixed = TRUE))
  expect_true(grepl("No. (%) or Mean", xml, fixed = TRUE))

  body <- ft$body$dataset
  expect_identical(body$label[1], "Demographics")
  expect_identical(body$n_stat_1[2], "392")
  expect_identical(body$n_stat_2[2], "133")
})
```

- [ ] **Step 2: Run test to verify it fails or passes**

Run: `Rscript -e 'devtools::load_all("."); testthat::test_file("tests/testthat/test-hv-man-table-jtcvs.R")'`
Expected: PASS immediately (Task 3's implementation already covers this shape) — this step exists to lock the real-template shape in as a regression guard, not to drive new implementation.

- [ ] **Step 3: Commit**

```bash
git add tests/testthat/test-hv-man-table-jtcvs.R
git commit -m "test: characterize hv_man_table_jtcvs() against template_jtcvs_consolidated.docx Table 1"
```

---

### Task 6: README + full package check

**Files:**
- Modify: `README.md`

**Interfaces:** None (docs + verification only).

- [ ] **Step 1: Add a JTCVS-format usage section to README.md**

Append after the existing `## Example` section:

```markdown
## JTCVS submission format

For JTCVS manuscript submission (merged spanning headers, matching Tess's
template), use `hv_man_table_jtcvs()` / `hv_man_table_save_jtcvs()` instead
of `hv_man_table()` / `hv_man_table_save()`:

``` r
library(gtsummary)
library(hvtiRtables)

tbl <- trial |>
  tbl_summary(
    by = trt,
    statistic = list(all_continuous() ~ "{N_obs} ||| {mean} ± {sd}"),
    include = c(age, grade)
  )

ft <- hv_man_table_jtcvs(
  tbl,
  groups = c(stat_1 = "Drug A (n=98)", stat_2 = "Drug B (n=102)")
)
hv_man_table_save_jtcvs(ft, "table1.docx", caption = "Table 1. Baseline Characteristics")
```
```

- [ ] **Step 2: Run the full package check**

Run: `Rscript -e 'devtools::check(document = FALSE)'`
Expected: `0 errors | 0 warnings`. Note any NOTEs in the summary rather than treating them as blocking (this plan is not the full CRAN release gate).

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: add JTCVS submission format usage section to README"
```

---

## Explicitly out of scope for this plan

- **`repair_table_structure()`** (Component 2 of the design spec) — a separate plan, since it addresses a different input (arbitrary/corrupted external `.docx` files) and a different open risk (flag-vs-auto-fix) than this plan's gtsummary-to-.docx pipeline.
- **Delivery mechanism** (who runs which function, R vs. Word-native) — deferred per the design spec.
- **Bold styling for categorical parent rows with their own N** (e.g. "Severity" in the template, which shows real N/Std.Diff. values, unlike a blank section header) — the template shows this row type styled distinctly from both plain level rows and full section headers, but the exact styling (bold vs. bold-italic, merged vs. not) wasn't confirmed against the template's run-level formatting during design. Follow-up task once confirmed, not blocking this plan's core structure.
- Named presets for standard table archetypes — per the design spec, deferred until real multi-manuscript usage shows recurring shapes.
