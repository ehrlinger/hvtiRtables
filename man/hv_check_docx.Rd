% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hv-check-docx.R
\name{hv_check_docx}
\alias{hv_check_docx}
\title{Check a .docx for structural patterns the house rules forbid}
\usage{
hv_check_docx(path)
}
\arguments{
\item{path}{Path to a \code{.docx} file.}
}
\value{
A \code{data.frame} with one row per finding and columns \code{type},
\code{table} (1-indexed table number, \code{NA} when document-level),
\code{location} (row/column detail where applicable, \code{NA} otherwise), and
\code{detail} (human-readable description). Zero rows means clean.
}
\description{
Read-only inspection of any Word document, whether written by this
package or received from elsewhere. Reports the three structural
failure patterns the canonical "Table Construction for Manuscripts"
memo names, and never modifies the file.
}
\details{
Three detectors run:
\itemize{
\item \code{"layer"} (structural, high confidence): paragraphs positioned with
\code{w:framePr}, or floating text boxes (\code{w:txbxContent}). Table content
belongs in the table, not in a floating layer over it.
\item \code{"hidden_column"} (structural, high confidence): a table column that
is both entirely empty \strong{and} narrower than 0.1 inch. Both
conditions are required. The house's own JTCVS templates contain
all-empty 0.5-inch gutter columns between group pairs, which are
deliberate; width alone or emptiness alone would flag them.
\item \code{"embedded_footnote"} (heuristic, lower confidence): a table's last
row containing text that starts with a footnote marker (\code{*}, a
dagger, a lettered \code{a.}/\code{b.} marker, or \verb{Key:}), the pattern
\code{flextable::footnote()} produces when misused. This detector is
pattern-based rather than a guaranteed structural signature, so
treat its findings as prompts to look, not proof.
}
}
\examples{
library(gtsummary)
tbl <- trial |>
  tbl_summary(
    by = trt,
    statistic = list(all_continuous() ~ "{N_obs} ||| {mean}"),
    include = age
  )
ft <- hv_man_table_jtcvs(tbl, groups = c(stat_1 = "A", stat_2 = "B"))
out <- tempfile(fileext = ".docx")
hv_man_table_save_jtcvs(ft, out, caption = "Table 1. X")
hv_check_docx(out)

}
\seealso{
\code{\link[=hv_man_table_save]{hv_man_table_save()}} and \code{\link[=hv_man_table_save_jtcvs]{hv_man_table_save_jtcvs()}}, which
run this check automatically on their own output.
}
