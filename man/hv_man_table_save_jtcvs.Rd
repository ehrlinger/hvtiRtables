% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hv-man-table-save-jtcvs.R
\name{hv_man_table_save_jtcvs}
\alias{hv_man_table_save_jtcvs}
\title{Write an hv_man_table_jtcvs() table to a compliant .docx}
\usage{
hv_man_table_save_jtcvs(
  ft,
  file,
  caption,
  footnotes = NULL,
  abbreviations = NULL,
  ...
)
}
\arguments{
\item{ft}{A \code{flextable}, from \code{\link[=hv_man_table_jtcvs]{hv_man_table_jtcvs()}}.}

\item{file}{Output \code{.docx} path. The output directory (\code{dirname(file)})
must already exist; this function does not create it. (\verb{\%summarytable}
\verb{RTFFILE=}/\verb{PDFFILE=} equivalent; output here is always \code{.docx}).}

\item{caption}{Full caption text, e.g. \code{"Table 1. Baseline Characteristics"}, rendered bold above the table. This function does
not auto-number tables for you; include the number yourself.
(\verb{\%summarytable} \verb{TBLTITLE=} equivalent).}

\item{footnotes}{Optional list of \code{list(row =, col =, text =)}, one per
footnote, in the order letters should be assigned (\code{a}, \code{b}, ...).
\code{col} is a single \code{col_keys} name. \code{row} may be a vector, in which
case the one letter marks every row named, which is how several rows
share a footnote. \code{row} indexes \code{ft}'s body rows as shown: for a
sectioned table (built with \code{groupname_col}), that includes the
section-header rows \code{\link[=hv_man_table_jtcvs]{hv_man_table_jtcvs()}} interleaves into the body,
so count those too. \code{\link[=hv_test_footnotes_jtcvs]{hv_test_footnotes_jtcvs()}} computes these
indices for you when the footnotes mark statistical tests.
(\verb{\%summarytable} \verb{ADDFN=} equivalent; \code{PRINTFN=1}'s house block is
\code{\link[=hv_man_footnotes]{hv_man_footnotes()}}).}

\item{abbreviations}{Optional named character vector, same as
\code{\link[=hv_man_table_save]{hv_man_table_save()}}, rendered via the shared \verb{Key:} helper.}

\item{...}{Not used. Present so that \verb{\%summarytable} parameter names
produce an error naming the argument to use instead.}
}
\value{
Invisibly, the \code{file} path.
}
\description{
Stage 3 of the \verb{\%summarytable} split: writes the document. Replaces
the macro's \verb{RTFFILE=}/\verb{PDFFILE=} output block.
}
\details{
JTCVS counterpart to \code{\link[=hv_man_table_save]{hv_man_table_save()}}: it adds a bold \verb{Table N. Caption} paragraph before the table, and renders footnotes as lettered
(\code{a.}, \code{b.}, ...) markers attached to specific body cells rather than
the fixed symbol set \code{\link[=hv_man_table_save]{hv_man_table_save()}} uses on the header row,
matching the JTCVS template convention.
}
\section{Common mistakes}{

\strong{"\verb{footnotes\[\[k\]\]$text} must be a single non-empty string."}
Every footnote needs all three of \code{row}, \code{col}, and \code{text}, and
\code{text} must be one non-empty string. Before this check existed, an
entry missing \code{text} wrote a document with a dangling superscript
marker and an empty footnote line.

\strong{"\code{footnotes[[k]]} must be a list of the form list(row =, col =,
text =)."} \code{footnotes} is a list \emph{of} footnotes, so a single one is
\code{list(list(row = 1, col = "n_stat_1", text = "..."))}. Dropping the
outer \code{list()} previously failed with \verb{$ operator is invalid for atomic vectors}.

\strong{"\code{footnotes[[k]]$row} must be whole numbers ..."} Row indices
count the section-header rows \code{\link[=hv_man_table_jtcvs]{hv_man_table_jtcvs()}} interleaves
into the body. \code{\link[=hv_test_footnotes_jtcvs]{hv_test_footnotes_jtcvs()}} computes them for you.

\strong{"\code{file} must be a single non-empty file path."} Check the
argument order: it is \verb{(ft, file, caption)}.

\strong{"Output directory does not exist: ..."} The directory part of
\code{file} (its \code{dirname()}) is not created for you; create it first
with \code{dir.create(dirname(file), recursive = TRUE)}.
}

\examples{
library(gtsummary)
tbl <- trial |>
  tbl_summary(
    by = trt,
    statistic = list(
      all_continuous() ~ "{N_obs} ||| {mean} ± {sd}",
      all_categorical() ~ "{N_obs} ||| {n} ({p}\%)"
    ),
    include = c(age, grade),
    missing = "no"
  )
ft <- hv_man_table_jtcvs(
  tbl,
  groups = c(stat_1 = "Drug A (n=98)", stat_2 = "Drug B (n=102)")
)
out <- tempfile(fileext = ".docx")
hv_man_table_save_jtcvs(
  ft, out,
  caption = "Table 1. Baseline Characteristics",
  footnotes = list(list(
    row = 1, col = "n_stat_1", text = "Patients with data available."
  ))
)

}
\seealso{
\code{\link[=hv_man_table_jtcvs]{hv_man_table_jtcvs()}} to build a compliant \code{flextable} first.
}
