% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hv-tbl-summary.R
\name{hv_tbl_summary}
\alias{hv_tbl_summary}
\title{Build a gtsummary table from a SAS \%summarytable-style grouped variable list}
\usage{
hv_tbl_summary(
  data,
  by = NULL,
  groups,
  continuous = character(0),
  binary = character(0),
  categorical = character(0),
  compare = c("pvalue", "smd", "both", "none"),
  percentiles = c(15, 85)
)
}
\arguments{
\item{data}{A data frame.}

\item{by}{Grouping variable name as a string (\verb{\%summarytable} \verb{CLASS=}
equivalent), or \code{NULL} for a single ungrouped "Overall" column.}

\item{groups}{Named list, section label -> variable names in display
order (\verb{\%summarytable} \verb{LIST=} equivalent), e.g.
\code{list(Demography = c("age", "female"), Symptoms = c("nyha"))}. Every
variable named here must appear in exactly one of \code{continuous},
\code{binary}, or \code{categorical}, and every classified variable must
appear in \code{groups}.}

\item{continuous}{Character vector of continuous variable names
(\verb{\%summarytable} \verb{CON1=} equivalent), summarized as
\verb{median (P<low>, P<high>)}.}

\item{binary}{Character vector of 0/1 variable names (\verb{\%summarytable}
\verb{CAT1=} equivalent), summarized as \verb{n (\%)} on a single row.}

\item{categorical}{Character vector of multi-level variable names
(\verb{\%summarytable} \verb{CAT2=} equivalent), summarized as \verb{n (\%)} per
level. Ordinal variables belong here too; this function does not
run a trend test (\verb{\%summarytable}'s \verb{ORD1=} distinction is not
preserved).}

\item{compare}{One of \code{"pvalue"} (default), \code{"smd"}, \code{"both"}, or
\code{"none"}. Ignored (treated as \code{"none"}) when \code{by} is \code{NULL}, since
there is nothing to compare. \verb{\%summarytable} \verb{PVALUES=}/\verb{ASD=}
equivalent.}

\item{percentiles}{Numeric vector of length 2, the low/high percentile
pair for continuous summaries. Default \code{c(15, 85)}, the
\code{\link[=hv_man_footnotes]{hv_man_footnotes()}} house convention (\verb{\%summarytable} \verb{PP=}
equivalent).}
}
\value{
A \code{gtsummary} object, ready for \code{\link[=hv_man_table_jtcvs]{hv_man_table_jtcvs()}}. See
Details for the \code{hv_stat_label}/\code{hv_trailing} attributes.
}
\description{
Thin wrapper over \code{\link[gtsummary:tbl_summary]{gtsummary::tbl_summary()}} modeled on the interface
biostats team members already know from the \verb{\%summarytable} SAS macro:
a grouped, ordered variable list (\code{groups}, the macro's \verb{LIST=}
equivalent) and variable-type buckets (\code{continuous}/\code{binary}/
\code{categorical}, the \verb{CON1=}/\verb{CAT1=}/\verb{CAT2=} equivalents), rather than
gtsummary's own tidyselect-based interface. Returns a plain \code{gtsummary}
object, ready for \code{\link[=hv_man_table_jtcvs]{hv_man_table_jtcvs()}}.
}
\details{
Every continuous variable is summarized as
\verb{median (P<low>, P<high>)} using a blanket non-parametric test
(Wilcoxon rank-sum for 2 groups, Kruskal-Wallis for 3+) — this
function does not classify variables as Gaussian/non-Gaussian the way
\verb{\%summarytable} does; that is \code{gtsummary::add_p()}'s own default
continuous test already. \code{percentiles} defaults to the house
convention documented in \code{\link[=hv_man_footnotes]{hv_man_footnotes()}} (15th/85th),
overridable per study (\verb{\%summarytable} equivalent: \verb{PP=}).

The returned object carries two attributes for \code{\link[=hv_man_table_jtcvs]{hv_man_table_jtcvs()}}:
\code{hv_stat_label}, the percentile-aware sub-header text
(\code{"No. (\%) or Median (<low>th, <high>th percentile)"}), and
\code{hv_trailing}, a named character vector ready to pass as
\code{\link[=hv_man_table_jtcvs]{hv_man_table_jtcvs()}}'s \code{trailing} argument when \code{compare} produced a
comparison column (\code{NULL} when \code{compare = "none"}).
}
\examples{
hv_tbl_summary(
  mtcars,
  groups = list(Engine = c("mpg", "cyl")),
  continuous = "mpg",
  categorical = "cyl"
)

}
\seealso{
\code{\link[=hv_man_table_jtcvs]{hv_man_table_jtcvs()}} to render the result.
\code{\link[=hv_man_footnotes]{hv_man_footnotes()}} for the percentile-footnote house convention.
}
