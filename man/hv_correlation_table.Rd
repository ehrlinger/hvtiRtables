% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hv-correlation-table.R
\name{hv_correlation_table}
\alias{hv_correlation_table}
\title{Pairwise correlations with Fisher confidence intervals}
\usage{
hv_correlation_table(
  data,
  vars,
  with = NULL,
  by = NULL,
  method = c("spearman", "pearson"),
  conf_level = 0.68,
  digits = 2
)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Character vector of numeric columns (\code{VAR} statement).}

\item{with}{Character vector of numeric columns to correlate each of \code{vars}
against (\code{WITH} statement). \code{NULL} (default) correlates every unordered
pair within \code{vars}.}

\item{by}{Optional single column name to stratify on (\code{BY} statement). Each
level gets its own rows; the column is carried first in the output.}

\item{method}{One or both of \code{"spearman"} and \code{"pearson"}.}

\item{conf_level}{Confidence level for the Fisher interval, strictly
between 0 and 1. Default \code{0.68}.}

\item{digits}{Decimal places in \code{display}. Default \code{2}, the SAS \code{6.2} format.}
}
\value{
A data frame, one row per stratum, pair and method, with columns
\code{by} (when given), \code{variable}, \code{label}, \code{with}, \code{method}, \code{n},
\code{estimate}, \code{conf.low}, \code{conf.high}, \code{p.value} and \code{display}
(\code{"r (lcl, ucl)"}, \code{NA} when the interval is undefined at n <= 3). The
level used is stored as \code{attr(x, "conf_level")}.
}
\description{
The table half of the SAS correlation job
\verb{proc corr nosimple spearman pearson fisher(biasadj=no alpha=.32)}, with
the \code{SpearmanCorr}, \code{FisherSpearmanCorr}, \code{PearsonCorr} and
\code{FisherPearsonCorr} ODS tables folded into one data frame. The scatter-plot
matrix (\code{plots=matrix}) is \code{hvtiPlotR::hv_correlation_matrix()}.
}
\details{
The interval is Fisher's z without bias adjustment, SAS's \code{biasadj=no}:
\code{z = atanh(r)}, \code{se = 1 / sqrt(n - 3)}, back-transformed with \code{tanh()}. The
same transform is applied to Spearman's coefficient, as SAS does. The
p-value tests rho = 0 on the same z scale, so it is the Fisher p-value SAS
reports alongside the interval, not the t-test p-value of
\code{\link[stats:cor.test]{stats::cor.test()}}. The interval, by contrast, is identical to
\code{cor.test()}'s for Pearson.

\code{conf_level} defaults to 0.68, the exemplar's \code{alpha=.32}: the house
one-standard-error convention, the same logic as the 15th and 85th
percentiles in \code{\link[=hv_tbl_summary]{hv_tbl_summary()}}. Pass \code{0.95} for a conventional interval.

Missing values are deleted pairwise, per pair and per stratum, which is
\verb{proc corr}'s default. Rows with a missing \code{by} value are dropped, where
SAS's \code{BY} would keep them as their own group.
}
\examples{
hv_correlation_table(
  datasets::mtcars, vars = c("hp", "wt", "qsec"), with = "mpg",
  by = "am"
)
}
\seealso{
\code{hvtiPlotR::hv_correlation_matrix()} for the plot.
}
