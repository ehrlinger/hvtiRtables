% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standard-footnotes.R
\name{standard_footnotes}
\alias{standard_footnotes}
\title{The house-standard manuscript table footnotes}
\usage{
standard_footnotes()
}
\value{
A named list with elements \code{`*`} and \code{`†`}, in the format
\code{\link[=save_manuscript_table]{save_manuscript_table()}}'s \code{footnotes} parameter expects.
}
\description{
Two footnotes are universal to the HVTI CORR "Table Construction for
Manuscripts" rules, not specific to any one study, so they live here
rather than being re-typed at every \code{\link[=save_manuscript_table]{save_manuscript_table()}} call site:
a \code{*} for the non-missing-value count (house rule 8, exact wording
required) and a \verb{†} explaining the \code{{median} ({p15}, {p85})} format used
for continuous variables throughout these tables. No footnote is needed
for the categorical \verb{n (\%)} format — the column header text already
covers it (house rules 10/12).
}
\details{
\code{\link[=save_manuscript_table]{save_manuscript_table()}}'s \code{footnotes} parameter defaults to calling
this function, so every table gets both automatically. Override with
ordinary list operations — no special sentinel values:
\itemize{
\item Suppress both: \code{footnotes = NULL}
\item Change one: \code{modifyList(standard_footnotes(), list(...))} with \code{`†`} = "custom text"
\item Add a study-specific one alongside: \code{c(standard_footnotes(), list(...))} with \code{`‡`} = "extra note"
}
}
\seealso{
\code{\link[=save_manuscript_table]{save_manuscript_table()}}
}
